# Development Evidence

The active candidate-selection sequence is:

1. `pilot21_feasibility_screen`
2. `pilot21_feasibility_screen_l01_120s`
3. `internal_screen_fast`
4. `confirmatory_adaptive`

These directories contain development evidence, not final paper results. Standard packages retain raw JSONL, CSV summaries, and reports without duplicate bulk arrays or generated solutions.
